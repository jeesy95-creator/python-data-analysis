# 프로그래머스 Lv2 388352 - 비밀 코드 해독

🔗 문제 링크  
https://school.programmers.co.kr/learn/courses/30/lessons/388352

---

## 문제 요약
1부터 n까지의 정수 중 서로 다른 5개로 이루어진 비밀 코드가 존재한다.  
m번의 시도를 통해 입력한 5개 숫자와 비밀 코드 간의 **일치 개수**가 주어질 때,  
조건을 만족하는 **가능한 비밀 코드 조합의 개수**를 구하는 문제이다.

---

## 접근 아이디어
- 비밀 코드는 **1~n 중 5개를 고르는 조합**이므로 모든 조합을 후보로 생성한다.
- 각 후보 조합에 대해 모든 시도(q)에 대해
  - 후보 코드와 입력 값의 **교집합 개수**가 응답(ans)과 같은지 확인한다.
- 모든 조건을 만족하는 후보 조합만 카운트한다.

---

## 핵심 포인트
- `itertools.combinations`를 이용한 조합 완전 탐색
- `set` 교집합을 이용해 일치 개수 계산
- `zip(q, ans)`를 사용해 쿼리와 응답을 동시에 순회

---

## 배운 점
- 완전 탐색 문제라도 **탐색 공간이 제한적이면 충분히 구현 가능**하다.
- `zip`을 사용하면 인덱스 실수를 줄이고 코드 가독성을 높일 수 있다.
- `set` 생성 위치를 조정하는 것만으로도 불필요한 연산을 줄일 수 있다.

---

## 시간 복잡도
- 조합 개수: C(n, 5) (최대 C(30, 5) ≈ 142,506)
- 각 조합마다 최대 m(≤10)번 검사  
→ 전체 시간 복잡도는 **O(C(n,5) × m)** 으로 제한 내에서 충분히 통과 가능
